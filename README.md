# Expense – Kubernetes Manifests

This repository contains Kubernetes YAML manifests to deploy the **Expense** application on a Kubernetes cluster. The stack includes:

- **MySQL** – Persistent backend database
- **Node.js Backend** – API and business logic
- **Nginx Frontend** – Static frontend web UI
- Namespace called expense
